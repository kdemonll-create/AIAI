# Core (recommended)
PyYAML>=6.0
psutil>=5.9

# Higher reasoning bridge (optional; only if llm.enabled and Ollama is running)
requests>=2.31

# Computer vision (optional; enables /camera, /motion, /image and visual novelty)
opencv-python>=4.8
numpy>=1.24

# Hardware / serial interface (optional; stays dry-run by default)
pyserial>=3.5

# Audio sense (optional; enables /audio)
sounddevice>=0.4

# Screen sense (optional; enables /screen). mss preferred, Pillow works as fallback.
mss>=9.0
Pillow>=10.0
